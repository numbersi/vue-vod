import Vue from 'vue'
import {
  Button,
  Carousel,
  CarouselItem,
  Row,
  Col,
  Card,
  Tag,
  Dialog,
  Input,
  Select,
  Option,
} from 'element-ui'


Vue
  .use(Button)
  .use(Carousel)
  .use(CarouselItem)
  .use(Row)
  .use(Col)
  .use(Card)
  .use(Tag)
  .use(Dialog)
  .use(Input)
  .use(Select)
  .use(Option)
// .use()